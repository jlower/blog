---
title: 2027秋招-携程二面
date: 2026-09-18 03:00:00
categories: [面经]
keywords: [面试经历,面经,优化点]
tag: []
description:
---

## 告警怎么才能画出拓扑图，怎么得到所有上下游服务的拓扑图节点？拓扑关系是会动态变化的，怎么自动感知业务关系的变动？怎么定位告警的根因？

告警是一个比较大，比较复杂的部分，其中管理拓扑图链路和排查告警根因是很难的，看看你有啥解决办法吗。

## 如果有多个应用，但CPU的核数只有一个，这时候他们怎么去抢占时间片的？

## CPU已经是很忙的状态了，水位达到80%，如果Web服务还不断的有请求过来，那CPU有可能爆满卡死，这样的话有什么办法把CPU的水位维持在80%不要再高？

## RPC怎么实现异步调用呢？

## 为什么用消息队列就能异步处理？怎么保证消息队列的可靠性？

## 如果再加上MySQL和Redis，原生Redis不支持事务，怎么保证写数据的时候能MySQL+Redis有类似事务？分布式事务/Redis缓存只保证最终一致性，柔性事务？

## 为什么RPC一般都是基于TCP而不是UDP的？TCP和UDP的区别是啥？为什么绝大多数场景都是用TCP而不是用UDP？

## TCP连接时，如果网络内有突然的流量洪峰的问题，TCP是怎么去化解的？拥塞控制目前的问题，算法缺陷？拥塞控制是不是只能约束发送方，如果服务端应用不去读socket的缓存，照样会有问题？拥塞控制是在已经建立好的TCP连接上才生效的，如果是SYN洪峰打满半连接队列的话无法处理吧？

TCP拥塞控制核心是能避免整个网络被冲跨，TCP我认为是一个比较复杂的应用，整体设计我们自己的业务应用时是有很大的借鉴价值的。

## 手撕：怎么计算 (1+2)*3 这个算式
