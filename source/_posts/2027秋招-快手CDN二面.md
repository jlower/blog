---
title: 2027秋招-快手CDN二面
date: 2026-09-14 03:00:00
categories: [面经]
keywords: [面试经历,面经,优化点]
tag: []
description:
---

## K8s网关的调度？K8s的网关和普通的网关有区别吗？K8s怎么转发请求到适合它的Pod的？

## 发布有什么类型？

## 为什么有的机器上只有一个Pod，这样直接用物理机不就好了，用Pod有什么作用？（主要是为了让老的物理机上云）一个机器上多个Pod？

## 一个Pod对应多个服务？（主要是为了让老的物理机上云）【现在新建的都是一个Pod对应一个服务】

## 为什么一个Pod对应一个服务、一个机器上部署多个Pod这样操作是主流，这样有什么好处？

## 用了K8s后性能有损耗吗？

## K8s的组成？

## 机器变更后CPU可能超容量吗？机器超卖怎么解决？热点打散用的是什么算法？热点打散K8s怎么调度的？

## 你这样打标签筛选有可能筛掉和变更相关的告警吗？

## Embedding模型计算向量的原理？不同模型可以混用吗，解析问题和文档向量化的模型要不一样吗？以后升级模型怎么办？

## Redis中向量是怎么存储的？ANN 检索，HNSW？多层跳表图中文档的向量是存在最下层图中吗？文档可能有多个语义，一个文档会对应多个向量吗？Transformer怎么解决多语义问题的？

## RAG的架构你具体讲讲？

## SSE和WebSocket的区别？WebSocket怎么实现的？

## SSE服务器不能主动推送给客户端，那客户端如果突然下线然后重启，为什么还能继续接收没接收完的东西？

## SSE会出现少字问题吗？怎么避免网络传递中字丢失的问题的？

## 注解主要是干啥的？注解有啥用？注解底层怎么实现的？Spring中有什么独有的注解？

## Spring的@Transaction能不能实现函数中前一段在事务里，后一段不在事务里？

## Spring的Bean的生命周期？Bean是怎么创建的？
