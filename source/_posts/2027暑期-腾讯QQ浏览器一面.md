---
title: 2027暑期-腾讯QQ浏览器一面
date: 2026-04-01 03:00:00
categories: [面经]
keywords: [面试经历,面经,优化点]
tag: []
description:
---

## 浏览器输入https链接后是什么流程

## https什么时候是对称加密，什么时候是非对称加密

## 【看】进程、线程、协程的区别？

## 【看】协程的效率为什么比线程高？

## 【看】协程和线程所占的空间大小？

## 【看】线程崩溃了后进程会不会崩溃？

## 【看】进程、线程、协程是怎么利用上多核的？Go语言是怎么多核高并发的？

## 【看】可以指定进程、线程、协程固定在某一个CPU核心上运行吗？怎么绑定？绑核有什么用？

## 【看】SQL有什么优化方式？

## 所有字段都建立索引的话会不会快一点呢？

## 最左前缀原则

## 慢查询怎么定位？

## MySQL的explain命令有啥用？

## 索引是什么数据结构实现的？主键索引是什么？

## MySQL存储引擎有哪些？InnoDB的特点是什么？

## 【看】MySQL自增ID用完了之后再插入数据还能插进去吗？怎么解决？

## 【看】MySQL里的锁有哪几种？间隙锁有了解吗？

## 【看】介绍一下脏读、幻读、不可重复读

## 【看】MySQL的事务隔离级别？ACID特性？怎么实现的？MVCC + 间隙锁 + 临键锁 能解决啥？

## 【看】Redis里面的字符串和C的字符串有啥区别（会记录字符串长度、总共分配了多少空间）？怎么判断当前这个字符串分配的内存够不够呢？

## 【看】Redis为什么支持高并发？

## IO多路复用有哪些？为什么用epoll？

## 【看】除了IO多路复用，Redis为什么支持高并发？

## 【看】Redis常用的数据结构？

## 【看】Redis怎么用来做消息队列、分布式锁、限流器？

## 【看】Redis怎么当缓存存数据？三层缓存是啥？（本地L1、分布式L2、数据库L3）怎么保证三层缓存数据一样？（注意本地缓存怎么让他失效）

## 缓存击穿、雪崩、穿透是啥？怎么解决？

## 【看】布隆过滤器里面判断为不在里面的就一定不在，判断为在里面的不一定在？

## 【看】Kafka里面的一条消息有可能被重复生产或重复消费吗？

## 【看】Kafka分区数大于消费者数时会咋样？什么是Kafka的重平衡？分区数小于消费者数时会咋样？

## 【看】如何解决Kafka消息堆积？

## 【看】Kafka里面的消息是可以保证顺序拉取的，但异步消费Kafka里面的消息的话会导致顺序乱掉

## 【看】怎么提升Kafka的吞吐量？

## 【看】Kafka的消息队列是已经存在的，消息管道里面有很多消息了，分区数也定了，这时候还能增加分区吗？

可以考虑：1、开启批量消费减少IO耗时 2、不加机器也不加分区，在消费者内部做多线程分片（按ID哈希分片、丢到多个内存队列里、每个内存队列一个线程消费【保证不乱序】）

## 【看】Linux查看文本的指令？一个文本文件有很多条记录（行），查看文件中同时包含“ABC”和“123”的记录（行）用什么命令？

grep、sed、awk；看看他们的具体用法和使用场景；看看正则表达式

## 【看】订单和库存的数据一致性是怎么保证的？是什么级别的一致性（最终一致性还是实时一致性）？

## 【看】订单智能释放使用了RocketMQ延迟消息+定时任务兜底方案，为什么不能只用RocketMQ延迟消息？

## 【看】意图判断和意图识别是怎么处理的？

## 【看】操作系统中页的作用？

## 【看】32位操作系统内存大小？加一条更大的内存条操作系统会怎么分配或调度？操作系统内存映射？

## 【看】10亿个QQ号找出最大的10个用什么数据结构？

## 【看】C++段错误原因？

## 【看】怎么停掉一个进程？有哪些信号，数字代表啥？

## 【看】golang调度协程相关的，golang是多线程模型还是多进程模型，golang线程和协程是一对多还是多对一还是多对多关系，想问的是golang的M：N模型，协程数远大于线程数

## 【看】go协程是否存在父子关系，在一个go协程起另一个go协程，这两个go协程是否有关系？如果在一个协程里开另一个协程，外面的协程能否捕获到里面协程的panic？

## 【看】协程如何抢占CPU时间？协程的调度器是怎么实现的？协程的调度算法是什么？

## 【看】协程的栈空间大小以及如何增长？协程和线程比为什么轻量级？进程线程调度算法？Linux目前默认实际使用的调度算法CFS完全公平调度是咋样的？

## 【看】golang的select会不会死锁？会死锁，怎么解决？

## 【看】如果epoll管理的套接字中很多都有大量的数据io，epoll的效率依旧很好吗？系统怎么判断套接字上有数据写入？

## 【看】rsa握手和ecdhe+ecdsa握手，问ecdhe+ecdsa握手怎么保证前向安全性的？0-RTT握手和1-RTT握手的区别？

## 【看】TCP连接在关闭前写入了一些数据，然后再发送fin报文，会不会跳过这些数据直接发送fin报文？（不会，因为tcp的特点就是保序）追问怎么可以跳过这些数据，关闭的时候直接发送fin报文？（面试官提示在初始化套接字的时候可以设置一个配置参数，SO开头的，是SO_LINGER）SO_LINGER具体怎么设置有啥用？

## 【看】怎么往系统里面存一个超大文件？（可能内存不够全读取）

## 在工作或学习中遇到一些技术困难怎么解决？

## 手撕：统计最大岛屿面积（DFS/BFS）
