---
title: 2027暑期-腾讯QQ浏览器二面
date: 2026-04-07 03:00:00
categories: [面经]
keywords: [面试经历,面经,优化点]
tag: []
description:
---

## 手撕：实现字符串比较大小的函数，忽略大小写（就是 strcasecmp 函数怎么实现，传入的字符串为const）

## 【看】如何保证支付接口幂等性？

## 【看】介绍一下分布式事务？（很多种都详细看看）

## 【看】假如业务执行还没完，分布式锁就被释放了怎么办？

## 【看】https如果有中间人介入怎么办？CA证书信任链问题？

## 【看】MySQL的主从数据同步怎么做？binlog里面具体是什么东西？

## 【看】MySQL性能调优有哪些方向？

## 【看】Redis高性能原理？Redis单线程但机器一般都是多线程，会不会造成资源利用率低？怎么解决？

## 【看】假设服务器CPU都跑满100%，你会怎么排查问题？用什么命令排查？火焰图有什么用，Arthas用什么命令？

## 【看】服务请求量很多并且请求出现了服务超时，但CPU的利用率很低，需要排查哪些方面？（线程池的核心线程数配置的太小、DB的连接池大小太小、文件描述符不够、线程数上限、内存不足、网络IO、等待数据库返回、磁盘IO、调用第三方接口时返回慢或超时、阻塞式锁的竞争严重、代码里sleep太久......还有很多）

## 【看】多线程任务的线程数开多少比较合适？

## 【看】怎么避免死循环，死锁？

## 【看】Linux用过哪些命令？怎么分析日志的？（top、ps、df、free、netstat、lsof、strace、perf、flamegraph等）

## 【看】雪花算法怎么实现？同一毫秒内同一机器上请求多个ID时，后面自增的字段需要加锁吗？有没有测试过雪花算法最多支持同时生成多少个ID？

## 【看】Kafka怎么保证数据的可靠性？

## 【看】如果Kafka生产者生产的速度远大于消费者消费的速度会出现啥问题？消费同一个topic的消费者中有的消费快、有的消费慢会有啥问题？

## 【看】Kafka中常见的问题？Kafka消息堆积、性能下降、数据丢失、重复消费、集群稳定性等问题怎么解决？

## 【看】告警平台能使用Agent的能力做些啥呢？

## 【看】市面上常见的大模型有哪些？分别有什么特点？

## 【看】实现Agent的时候都是用同一个模型吗？（意图识别用 Embedding 模型【向量模型】把用户输入的话转成向量，然后RAG根据向量查相关性高的文档部分；工具选择可以使用MCP协议；生成回答使用全量的基础大模型）
